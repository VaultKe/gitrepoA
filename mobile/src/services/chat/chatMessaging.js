// chatMessaging.js - minimal shim retained for compatibility.
// All chat logic now lives in ChatService.js.
export function attachMessaging(prototype) {
  // No-op: ChatService already implements messaging methods directly.
}
